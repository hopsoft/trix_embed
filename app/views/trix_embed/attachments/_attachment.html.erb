<%= tag.figure class: "attachment attachment--preview" do %>
  <%# (local_assigns[:attachment] || local_assigns[:trix_embed].try(:attachable))&.content&.html_safe || "It didn't work!!!" %>
  <%= attachment.content.html_safe %>
<% end %>
