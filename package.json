{
  "author": "Nate Hopkins (hopsoft) <natehop@gmail.com>",
  "name": "trix-embed",
  "version": "0.0.1",
  "repository": "https://github.com/hopsoft/trix_embed",
  "description": "A Stimulus controller to safely embed external media in the Trix editor",
  "keywords": [
    "actiontext",
    "stimulus",
    "trix",
    "trix-editor",
    "wysiwyg"
  ],
  "main": "app/assets/builds/trix-embed.js",
  "files": [
    "dist/*"
  ],
  "license": "MIT",
  "dependencies": {},
  "peerDependencies": {
    "@hotwired/stimulus": ">= 3.2",
    "trix": ">= 2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "http-server": "^14.1.1",
    "prettier": "^3.0.0"
  },
  "scripts": {
    "build:all": "bundle && yarn && bin/standardize && rake build && node bin/build.mjs",
    "build:js": "yarn && node bin/build.mjs",
    "build:ruby": "bundle && rake build",
    "dev": "bin/run -c-1"
  }
}
