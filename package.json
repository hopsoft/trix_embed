{
  "name": "trix-embed",
  "version": "0.0.0",
  "main": "dist/trix-embed.js",
  "license": "MIT",
  "private": true,
  "dependencies": {},
  "peerDependencies": {
    "@hotwired/stimulus": ">= 3.2",
    "trix": ">= 2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "http-server": "^14.1.1",
    "prettier": "^3.0.0"
  },
  "scripts": {
    "build": "bin/standardize && yarn && node bin/build.mjs",
    "dev": "bin/run -c-1"
  }
}
